table:
  name: orders
  schema: public
array_relationships:
  - name: transactions
    using:
      foreign_key_constraint_on:
        column: order_id
        table:
          name: transactions
          schema: public
